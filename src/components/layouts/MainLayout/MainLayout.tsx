import { FormTemplate, TableTemplate } from 'components';

const MainLayout = () => {
    return (
        <div>
            <FormTemplate />
            <TableTemplate />
        </div>
    );
};

export default MainLayout;
